interface:
  display_name: "Caveman"
  short_description: "Brief answers without losing technical meaning"
  default_prompt: "Use $caveman in full mode and preserve every technical invariant."
policy:
  allow_implicit_invocation: true
