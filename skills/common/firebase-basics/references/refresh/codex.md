# Refresh Codex Local Environment

Follow these steps to refresh Codex's local environment, ensuring that their agent skills and plugins are fully up-to-date.

Use Codex plugin/skill workflow instead of `npx`.

1. **Update the Plugin:** Run the specific CLI command to update the Firebase plugin:
   ```bash
   claude plugin update firebase@firebase
   ```
